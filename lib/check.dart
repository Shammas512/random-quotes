import 'package:flutter/material.dart';

class Checkd extends StatelessWidget {
  const Checkd({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Heeloooo") ,
      ),
    );
  }
}